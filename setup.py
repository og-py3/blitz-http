"""
setup.py — blitz package installation.

Prefer installing via:
    pip install -e .
or
    pip install .

This file is kept for legacy tool compatibility; pyproject.toml is the
canonical metadata source.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
